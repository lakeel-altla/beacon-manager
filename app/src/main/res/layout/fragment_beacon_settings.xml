<?xml version="1.0" encoding="utf-8"?>
<ScrollView
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

    <LinearLayout
            android:id="@+id/layout"
            android:orientation="vertical"
            android:paddingStart="8dp"
            android:paddingEnd="16dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

        <TextView
                android:id="@+id/textView_status_description"
                android:paddingTop="8dp"
                android:paddingStart="8dp"
                android:paddingEnd="16dp"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"/>

        <!-- Type -->
        <include layout="@layout/settings_item" android:id="@+id/type"/>

        <include layout="@layout/divider"/>

        <!-- Id -->
        <include layout="@layout/settings_item" android:id="@+id/id"/>

        <include layout="@layout/divider"/>

        <!-- BeaconStatus -->
        <include layout="@layout/settings_item" android:id="@+id/status"/>

        <include layout="@layout/divider"/>

        <!-- Description -->
        <include layout="@layout/settings_item" android:id="@+id/description"/>

        <include layout="@layout/divider"/>

        <!-- Place HEX_ID -->
        <include layout="@layout/settings_item" android:id="@+id/place"/>

        <include layout="@layout/divider"/>

        <!-- Floor Level -->
        <include layout="@layout/settings_item" android:id="@+id/floorLevel"/>

        <include layout="@layout/divider"/>

        <!-- Expected stability -->
        <include layout="@layout/settings_item" android:id="@+id/stability"/>

        <include layout="@layout/divider"/>

        <!-- Property -->
        <TableLayout
                android:id="@+id/propertyLayout"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:stretchColumns="0">

        </TableLayout>

        <include layout="@layout/divider"/>

        <!-- Attachment -->
        <TableLayout
                android:id="@+id/attachmentLayout"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:stretchColumns="0">

        </TableLayout>

    </LinearLayout>
</ScrollView>